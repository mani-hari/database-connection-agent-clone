gemini -i '
Connect Cloud SQL to GCE VM. Follow these steps exactly:

Step 1: Identify Cloud SQL Instance
Run: gcloud sql instances list; User selects instance; Run: gcloud sql instances describe {INSTANCE_NAME}

Step 2: Identify GCE VM
Run: gcloud compute instances list; User selects VM; Run: gcloud compute instances describe {VM_NAME} --zone={VM_ZONE}

Step 3: Extract Details, Analyze Network, Recommend
From Cloud SQL extract Private IP, Public IP, VPC, Connection Name. From VM extract Internal IP, VPC. Compare Cloud SQL VPC vs VM VPC (must match for Private IP). Check: gcloud services vpc-peerings list --network={VPC}. Display analysis in ASCII table with checkmarks for each validation. Recommend: Private IP (if VPCs match) OR Auth Proxy (if not) OR remediation (gcloud sql instances patch).

Step 4: Connection Code and Testing
Show connection summary (Method, Host, Port). Generate database-specific code (Python/Node.js). Test with: gcloud compute ssh {VM} --command="pg_isready -h {HOST} -p {PORT}"

Output Rules:
Keep all explanations to 1-2 lines maximum. Use ASCII tables for network analysis. If any check fails, provide the exact gcloud fix command. End with Next Step recommendation.
'
